# Test Environment Variables
NODE_ENV=test

# Database (will be overridden by in-memory MongoDB in tests)
MONGO_URI=mongodb://localhost:27017/si3-backend-test

# Redis (mocked in tests)
REDIS_URL=redis://localhost:6379

# JWT
JWT_SECRET=test-super-secret-jwt-key-for-testing-only
JWT_EXPIRES_IN=7d

# Sanity CMS (test values)
SANITY_PROJECT_ID=test-project-id
SANITY_DATASET=test
SANITY_API_TOKEN=test-api-token

# Email Service (disabled in tests)
SMTP_USERNAME_EVENTS=test-events@si3.space
SMTP_TOKEN_EVENTS=test-smtp-token
SMTP_SERVER=smtp.protonmail.ch
SMTP_PORT=587
DISABLE_EMAIL_SENDING=true

# Server
PORT=8081
FRONTEND_URL=http://localhost:3000

# Test-specific settings
TEST_TIMEOUT=30000
